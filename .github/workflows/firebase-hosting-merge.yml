# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm i
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        env:
          VITE_FIREBASECONFIG_API_KEY: '${{ secrets.FIREBASECONFIG_API_KEY }}'
          VITE_FIREBASECONFIG_AUTH_DOMAIN: '${{ secrets.FIREBASECONFIG_AUTH_DOMAIN }}'
          VITE_FIREBASECONFIG_PROJECT_ID: '${{ secrets.FIREBASECONFIG_PROJECT_ID }}'
          VITE_FIREBASECONFIG_STORAGE_BUCKET: '${{ secrets.FIREBASECONFIG_STORAGE_BUCKET }}'
          VITE_FIREBASECONFIG_MESSAGING_SENDER_ID: '${{ secrets.FIREBASECONFIG_MESSAGING_SENDER_ID }}'
          VITE_FIREBASECONFIG_APP_ID: '${{ secrets.FIREBASECONFIG_APP_ID }}'
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VOTE_47A25 }}'
          channelId: live
          projectId: vote-47a25
